#include"part_1.cpp"
//using pointer implemetation of the queue 
int enqueue_ordred(Queue & Q, int e){
//instattionation :
ptrQueue::queue q;
//we allocate a new node :
node* newnode=new node;
newnode->element=e;
//the qu is sorted so we need to inert the e into a appropriate position 
//now we do the sorting :
//we traverse comparing each element of the node to the e 
//traversing :
node* temp=f;
//the queue is sorted in decreasing ordere :max---->min
while (f!=NULL){
    if (temp->element<e){temp->next=}
}
}

